import React from 'react';
import { ErrorTitle, ErrorWrapper } from '../NotFoundPage/styled';

const UserNotFound = () => (
  <ErrorWrapper>
    <ErrorTitle>User not found </ErrorTitle>
  </ErrorWrapper>
);

export default UserNotFound;
